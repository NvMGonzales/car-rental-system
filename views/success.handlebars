<div class="container">
    <div class="col-sm"></div>
    <div class="col-sm-4">
        <h3 class="green">Success!</h3>
        <p class="green">Thank you for your Purchase!</p>
        <p>Customer email: {{charge.source.name}}</p>
        <p>Amount: Php{{budget.total}}</p>
        <p>Card Ending: {{charge.source.last4}}</p>
        <p>Card: {{charge.source.brand}}</p>
        <a href="/">Go to Profile</a>
    </div>
    <div class="col-sm"></div>
</div>